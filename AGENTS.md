# AGENTS.md

## プロジェクト概要

KAZUE HANADA の個人ポートフォリオサイト制作プロジェクト。

Web Designer / Visual Designer として、
ファッション、EC、Web デザイン、グラフィック、写真・動画などの制作実績を掲載する。

---

## デザインの基準

- Figma を、レイアウト・余白・タイポグラフィ・配色・構成の基準とする。
- Figma のデザインを忠実に実装する。
- 明示的な指示がない限り、レイアウトを勝手に簡略化・再解釈・再設計しない。
- SP と PC は、それぞれ独立したレイアウトとして考える。
- PC 版は SP 版の単純拡大にしない。

---

## 使用技術

- HTML
- CSS
- JavaScript

CMS 制約はない。

必要に応じて、以下のライブラリ・機能を使用してよい。

- GSAP
- ScrollTrigger
- Swiper
- IntersectionObserver
- SVG アニメーション
- CSS アニメーション
- position: sticky
- clip-path
- CSS variables

ライブラリを追加する場合は、目的を明確にし、必要最小限にする。

---

## フォント

### デザインアクセント

- TT Cometus

### 英文・UI

- TT Commons Pro

### 日本語

- Zen Kaku Gothic New

フォントは Figma の指定を優先する。

---

## デザイン方向性

サイト全体の方向性は以下。

- Fashion Editorial
- Playful
- Handmade
- Refined
- Visual-first

一般的な Web デザイナーのポートフォリオではなく、
ファッションブランド、ファッション誌、クリエイティブスタジオの Web サイトのような見せ方を目指す。

作品画像を主役にする。

---

## 避けるデザイン

以下の表現は、明示的な指示がない限り使用しない。

- 均一なカード UI
- 単調な 3 カラムグリッド
- 過度な角丸
- グラデーション
- ダッシュボード風 UI
- テンプレート感の強いポートフォリオデザイン
- 意味の薄い英語コピー
- 過度なセリフ体・イタリック体
- AI 生成サイトにありがちな巨大タイポグラフィの多用
- 必要以上のアニメーション
- 作品より UI が目立つ構成

---

## レイアウト

- モバイルファーストで実装する。
- SP デザインは 375px 幅を基準とする。
- 375px 固定のレイアウトとして実装しない。
- SP の基本左右余白は Figma 指定を優先する。
- width / margin / padding / gap / position などは、画面幅に応じて自然に変化するよう実装する。
- Figma 上の px 値は基準値として扱い、必要に応じて `%` / `vw` / `calc()` / `clamp()` へ変換する。
- 例：375px 基準で 42px の値は、必要に応じて `calc(42 / 375 * 100vw)` のように換算する。
- 画像幅は固定 px を避け、`%` / `vw` / `calc()` などを使用して可変にする。
- 必要に応じて `max-width` を設定し、過度に拡大しないよう制御する。
- セクションの `height` は原則固定しない。
- 高さはコンテンツ量に追従させる。
- ビジュアル比率を維持する場合は `aspect-ratio` を使用する。
- Figma の height 値を、そのまま CSS の固定 height として使用しない。
- SP のフォントサイズも 375px 時の Figma 値を基準とする。
- 必要に応じて `vw` / `calc()` / `clamp()` を使用し、375px 以外の画面幅でも文字サイズとレイアウトのバランスが崩れないようにする。
- PC では画像サイズ・余白・配置に変化をつけ、エディトリアルな構成にする。
- 整ったグリッドをベースにしつつ、作品ごとに視覚的なリズムをつける。
- 雑誌風に見せるためだけに、要素を無秩序に配置しない。

---

## HTML 構造ルール

- 同一内容のテキストを、SP 用・PC 用として HTML に重複して記述しない。
- SP / PC の違いは原則として CSS で制御する。
- レスポンシブ対応のためだけに、同じ `<h1>`、`<h2>`、`<p>` などを 2 つ作らない。
- HTML はセマンティックかつ可能な限り単一の構造を維持する。
- SP と PC で改行位置だけが異なる場合は、テキスト自体を複製せず、必要最小限の `<br>` とクラスで制御する。
- レイアウト都合だけで DOM を二重化しない。
- 同じ内容を複数箇所で管理する構造は避ける。

---

## アニメーション

アニメーションは積極的に使用してよいが、
サイトの世界観を補助するために使用する。

使用候補：

- フェード
- 画像のわずかな移動
- scale
- hover アニメーション
- スクロール連動
- SVG の線描画
- 手描き線の描画アニメーション
- sticky を利用した演出
- 画像切り替え
- テキストアニメーション

### アニメーション実装ルール

- 見た目を再現できる、最もシンプルな実装を優先する。
- CSS だけで実現できる動きに JavaScript を使用しない。
- JavaScript / GSAP を使う場合も、処理を必要最小限にする。
- 1 つの単純な演出のために長大な JavaScript を書かない。
- 実装が複雑になった場合は、そのまま進めず、より単純な方法へ書き直せないか検討する。
- スクロールイベント内で重い DOM 計算やスタイル変更を繰り返さない。
- スクロール連動には必要に応じて GSAP ScrollTrigger を使用する。
- vanilla JavaScript でスクロール監視する場合は、`requestAnimationFrame` などを用いて負荷を抑える。
- アニメーション対象は原則として `transform` / `opacity` を優先する。
- `top` / `left` / `width` / `height` など、レイアウト再計算を頻発させるプロパティのアニメーションは避ける。
- 同じ値を毎フレーム DOM から再取得しない。
- 不要なイベントリスナーやタイマーを増やさない。
- アニメーション用コードは、何をしているか追いやすい構造にする。
- 実装後は Chrome / Safari / iPhone でカクつきがないか確認する。
- カクつきがある場合は、演出を増やすのではなく、まず処理を簡略化・最適化する。

以下は避ける。

- すべての要素を動かす
- スクロールのたびに過剰な演出を入れる
- 操作性を損なう動き
- 読み込みを重くするだけの演出
- 単純な動きに対する過剰な JavaScript
- 同じ演出のために複数の監視処理を重ねる

---

## 画像・動画

### 保存場所

- 画像：`/assets/images/`
- 動画：`/assets/video/`
- SVG：`/assets/svg/`

### 画像

- WebP / AVIF を優先する。
- 必要に応じて JPEG / PNG も使用可。
- 表示サイズに対して過度に大きな画像を使用しない。
- `width` / `height` または `aspect-ratio` を指定し、CLS を防ぐ。

### 動画

- 短尺・軽量な動画はローカルファイルとして使用可。
- 大容量動画は必要に応じて外部ホスティングも検討する。
- 自動再生時は原則 `muted` / `playsinline` を使用する。

---

## コーディングルール

- セマンティック HTML を使用する。
- CSS はセクション単位で整理する。
- JavaScript は役割ごとに整理する。
- アニメーション処理は可読性を保つ。
- 不要な DOM 操作を増やさない。
- レスポンシブ対応を必ず行う。
- Safari / Chrome / iPhone で確認する。
- パフォーマンスとアクセシビリティを考慮する。
- 既存コードを必要以上に複雑化しない。
- 修正時は、局所的な変更で済む場合に大規模な書き換えを行わない。
- 同じ目的の CSS / JavaScript を重複して追加しない。

---

## Figma からの実装

- Figma の数値を基準にする。
- Figma の px 値は「その画面幅での基準値」として解釈し、固定値として機械的に実装しない。
- フォントサイズ、行間、余白、画像サイズ、位置関係を可能な限り忠実に再現する。
- SP は 375px 時のデザインを基準にし、画面幅の変化に追従する実装にする。
- PC と SP の Figma がある場合は、それぞれを基準にし、中間幅は自然につながるよう補間する。
- Figma 上のアニメーション指定がある場合は、その意図を実装に反映する。
- Figma にない要素を独自判断で追加しない。
- 判断が必要な場合は、勝手に決めず確認する。
- 実装後は、Figma と比較して以下を自己チェックする。
  - 余白
  - 文字サイズ
  - 行間
  - 改行位置
  - 画像サイズ
  - 画像比率
  - 要素の位置
  - SP / PC の切り替わり
  - アニメーションの滑らかさ
- Figma に合わせるためだけに HTML を二重化しない。
- デザイン再現とレスポンシブ性を両立する。

---

## 実装時の優先順位

実装判断に迷った場合は、以下の順番を優先する。

1. Figma のデザイン意図
2. シンプルで保守しやすい HTML / CSS / JavaScript
3. レスポンシブ性
4. アニメーションの滑らかさ
5. パフォーマンス
6. Safari / iPhone を含む実機での安定性

見た目が再現できても、コードが過度に複雑になる方法は採用しない。

---

## Git

- このプロジェクトフォルダ外のファイルを編集しない。
- コミットメッセージは内容が分かる簡潔なものにする。
- 大きな修正前には必要に応じてコミットを行う。
- 公開用ファイルに不要な作業ファイルを含めない。

---

## 公開

公開先は GitHub Pages を想定する。

静的サイトとして、
HTML / CSS / JavaScript / 画像 / SVG / 動画を管理する。

将来的に独自ドメインへ変更できる構成にする。
